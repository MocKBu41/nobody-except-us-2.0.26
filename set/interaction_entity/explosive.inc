{"mine"		{inflammation_time 3}		{on spawn			{volumes "body" disable all}			{volumes "body1" disable all}			{add_view "mined_view_own"		"mined_own"		"mined_view"}			{add_view "mined_view_enemy"	"mined_enemy"	"mined_view"}			{add_view "mined_view_defused"	"defused"		"mined_view"}			{call "change_state"}		}		{on "_explosion"			{volumes "body1" disable blast}			{volumes "body" disable blast}			{stuff_detonate}		}		{on "explosion"			{if mined				{call "_explosion"}			}		}		{on "change_state"			{if mined				{tags add "mine_planted"}				{volumes "body1" disable all}				{volumes "body" enable select touch contact contact_ground blast}				{bone show "string"}				{bone show "string1"}				{bone show "string2"}				{volumes "detector" enable all}			 else tagged "manual_install"				{tags add "mine_planted"}				{volumes "body1" disable all}				{volumes "body" enable select touch contact contact_ground blast}				{bone show "string"}				{bone show "string1"}				{bone show "string2"}				{volumes "detector" enable all}			else				{tags remove "mine_planted"}				{volumes "body" disable all}				{volumes "body1" enable select touch contact contact_ground blast}				{bone hide "string"}				{bone hide "string1"}				{bone hide "string2"}				{volumes "detector" disable all}			}		}		{on blast_hit			{if min_energy 2.5				{if not stuff "mine"					{stuff_detonate}				}			}						}		{on "check_terrain"			{tags remove "mine_planted_road"}			{tags remove "mine_planted_ground"}			{if tagged "mine_planted"				{if tagged "vis_none"				else tagged "vis_25"  {if rand 0.25					{if terrain_fx "road" {tags add "mine_planted_road"}					else terrain_fx "country_road" {tags add "mine_planted_road"}					else terrain_fx "ground" {tags add "mine_planted_ground"}					}				}				else tagged "vis_50"  {if rand 0.5					{if terrain_fx "road" {tags add "mine_planted_road"}					else terrain_fx "country_road" {tags add "mine_planted_road"}					else terrain_fx "ground" {tags add "mine_planted_ground"}					}				}				else tagged "vis_75"  {if rand 0.75					{if terrain_fx "road" {tags add "mine_planted_road"}					else terrain_fx "country_road" {tags add "mine_planted_road"}					else terrain_fx "ground" {tags add "mine_planted_ground"}					}				}				else					{if terrain_fx "road" {tags add "mine_planted_road"}					else terrain_fx "country_road" {tags add "mine_planted_road"}					else terrain_fx "ground" {tags add "mine_planted_ground"}					}				}			}		}		{on "sp_spawn_obb"			{spawn "obstacle_avoid_mine"}			{tags remove "mine_planted_road"}		}	}		{"kamikaze_bomb"		{on blast_hit			{if min_energy 0.5				{stuff_detonate}			}			}	}		{"mine_antipersonnel"		{on contact			{if mine_effector "human"				{call "explosion"}			else mine_effector "vehicle"				{call "explosion"}			else mine_effector "animal"				{call "explosion"}			}		}		(mod "mp"			{on blast_hit				{if min_energy 1.5					{if not stuff "mine"						{stuff_detonate}					}				}				}		)	}	{"frogmine"		{on spawn			{volumes "body" disable all}			{volumes "body1" disable all}			{add_view "mined_view_own"		"mined_own"		"mined_view"}			{add_view "mined_view_enemy"	"mined_enemy"	"mined_view"}			{add_view "mined_view_defused"	"defused"		"mined_view"}			{call "change_state"}		}		{on "change_state"			{if mined				{tags add "mine_planted"}				{volumes "body1" disable all}				{volumes "body" enable select touch contact contact_ground blast}			 else				{tags remove "mine_planted"}				{volumes "body" disable all}				{volumes "body1" enable select touch contact contact_ground blast}			}		}		{on contact		  {if volume "body"			{if mine_effector "human"				{spawn "frogmine_jumper"}				{stuff_detonate}			else mine_effector "vehicle"				{spawn "frogmine_jumper"}				{stuff_detonate}			else mine_effector "animal"				{spawn "frogmine_jumper"}				{stuff_detonate}			}		  }		}		{on blast_hit			{if min_energy 3				{if not stuff "mine"					{stuff_detonate}				}			}			}	}	{"mine_manual_install"		{on spawn			{call "arm"}			{physics_work 0}		}		{on "arm"			{delay 3				{tags add "armed"}				{tags add "manual_install"}				{call "change_state"}			}			}		{on "explosion"			{call "_explosion"}		}	}	{"frogmine_manual_install"	                               		{on spawn			{delay 3				{volumes "body1" enable select touch contact contact_ground blast}				{volumes "body" enable select touch contact contact_ground blast}				{player -1}			}		}	}	{"flashmine"		{on spawn			{volumes "body" disable all}			{volumes "body1" disable all}			{add_view "mined_view_own"		"mined_own"		"mined_view"}			{add_view "mined_view_enemy"	"mined_enemy"	"mined_view"}			{add_view "mined_view_defused"	"defused"		"mined_view"}			{call "change_state"}		}		{on "change_state"			{if mined				{tags add "mine_planted"}				{volumes "body1" disable all}				{volumes "body" enable select touch contact contact_ground blast}			 else				{tags remove "mine_planted"}				{volumes "body" disable all}				{volumes "body1" enable select touch contact contact_ground blast}			}		}		{on contact		  {if volume "body"			{if mine_effector "human"				{spawn "flashmine_jumper"}				{stuff_detonate}			else mine_effector "vehicle"				{spawn "flashmine_jumper"}				{stuff_detonate}			else mine_effector "animal"				{spawn "flashmine_jumper"}				{stuff_detonate}			}		  }		}		{on blast_hit			{if min_energy 3				{if not stuff "mine"					{stuff_detonate}				}			}			}	}	{"flashmine_manual_install"	                                		{on spawn			{delay 3				{volumes "body1" enable select touch contact contact_ground blast}				{volumes "body" enable select touch contact contact_ground blast}				{player -1}			}		}	}	{"frogmine_jumper"		{on spawn			{impulse up 14 com}			{delay 0.1				{spawn "ex_grenade_big"}				{stuff_detonate}			}		}	}	{"flashmine_jumper"		{on spawn			{link_sound "launch" "weapon/shot/bazooka/SMAW"}			{play_sound "launch"}			{impulse up 14 rnd_dir 3}			{gravity 1.3}			{tags add "flare"}			{add_view "signal_rocket" "flare" "root"}			{view start "flare"}			{delay 15.5				{delete}			}		}		{on ground_hit				{stuff_detonate}		}		{on contact				{stuff_detonate}		}	}		         	                                            	{"grenade_mine"		{on spawn			{tags add "mine_planted"}			{volumes "detector" disable all}			{delay 2				{volumes "detector" enable contact blast touch}				{volumes "select" enable touch}			}		}		{on contact			{if rand 0.3 {with_effector {spawn "grenade_mine_alarm"}{with_effector}}}			{call "detonate"}		}		{on "detonate"			{volumes "detector" disable all}			{delay 0.75 0.25			     {with_linked_entity "grenade"				{stuff_detonate}			     }			     {delete}			}		}		{on blast_hit			{if min_energy 2	                          				     {with_linked_entity "grenade"						{stuff_detonate}				     }				     {delete}	              	                       	          			}			}	}		{"grenade_mine_alarm"		{on spawn			{delay 0.1				{explosive 1}				{able "burning" 1}			}			{delay 3				{explosive 0}				{able "burning" 0}				{if tagged "15s_timer"					{delay 12 {delete}}				else					 {delete}				}			}					}	}	{"grenade_gas_alarm"		{on spawn			{delay 0.1				{explosive 1}				{able "burning" 1}			}			{delay 3				{if tagged "15s_timer"					{delay 12 {delete}}				else					 {delete}				}			}					}	}		{"grenade_mine_f1" 		{on "demine"			{spawn "f1c"}			{delete}		}	}	{"grenade_mine_rgd" 		{on "demine"			{spawn "rgd"}			{delete}		}	}	{"grenade_mine_m61" 		{on "demine"			{spawn "m61_grenade"}			{delete}		}	}	{"grenade_mine_m67" 		{on "demine"			{spawn "m61_grenade"}			{delete}		}	}	{"grenade_mine_rgo" 		{on "demine"			{spawn "rgo"}			{delete}		}	}	{"grenade_mine_rgn" 		{on "demine"			{spawn "rgn"}			{delete}		}	}	{"grenade_mine_of37" 		{on "demine"			{spawn "of37_grenade"}			{delete}		}	}	{"grenade_mine_dm51gr" 		{on "demine"			{spawn "dm51gr"}			{delete}		}	}	{"grenade_mine_l109a1" 		{on "demine"			{spawn "l109a1"}			{delete}		}	}	{"grenade_mine_urg86" 		{on "demine"			{spawn "urg86"}			{delete}		}	}	{"grenade_mine_type73" 		{on "demine"			{spawn "type73"}			{delete}		}	}	{"grenade_mine_type86p" 		{on "demine"			{spawn "type86p"}			{delete}		}	}	{"grenade_mine_shgr56" 		{on "demine"			{spawn "shgr56"}			{delete}		}	}	                                                    	{"air_dropped_mine"		{inflammation_time 3}		{on spawn			{volumes "body" disable all}			{volumes "body" enable contact_ground}			{delay 1 {volumes "body" enable all}}			}		{on "burn_volume"			{delay 5 {call "explosion"}}		}			{on "explosion"			{volumes "body" disable blast}			{stuff_detonate}		}		{on ground_hit			{delay 1 {tags add "mine_planted"}}		}		{on contact		      {if not tagged "mine_planted"			{delay 1 {tags add "mine_planted"}}		      else			{call "check_effector"}		      }		}	}		{"pfm-1"		{on "check_effector"			{call "explosion"}		}	}		{"ptm-1"		{on "check_effector"			{if effector "vehicle"			      {if terrain_fx "sand"			                                                               				{if rand 0.8 {call "explosion"}}			      else terrain_fx "snow"				{if rand 0.6 {call "explosion"}}			      else terrain_fx "water"				{if rand 0.4 {call "explosion"}}			     else				{call "explosion"}			     }			}		}		{on "explosion" overload			{volumes "body" disable blast}			{delay 0 {stuff_detonate}}		}	}		                                                    	{"mine_antitank"		{on contact			{if mine_effector "mine_exploder"				{delay 0					{call "explosion"}				}			else mine_effector "vehicle"				{if mined					{volumes disable contact}					{delay 0.1						{call "explosion"}					}				}			}		}	}	{"mine_jumping"		{on "explosion"			{if mined				{volumes disable contact blast}				{call "_explosion"}			}		}		{on "_explosion" overload			{spawn "ex_bul_sm" "root" x}			{delay 0.01{stuff_detonate}}		}	}	{"mine_jumping_impactor"		{on spawn			{impulse up 14 com}			{delay 0.1				{stuff_detonate}			}		}	}	{"mine_antipersonnel_pob"	                                                                                           		{on contact overload			{if mine_effector "vehicle"				{if volume "body" {volumes disable contact}				else volume "body1" {volumes disable contact}				else {inherited}				}			else {inherited}			}		}		{on "_explosion"			{spawn "mine_antipersonnel_pob_j" "root" x}		}	}	{"mine_antipersonnel_ozm72"	                                                                                           		{on "_explosion"			{spawn "mine_antipersonnel_ozm72_j" "root" x}		}	}	{"mine_directional"		{on "_explosion" overload			{volumes disable all}			{spawn "blast_restrictor_apmine" "blast_dir" x}			{spawn "trophy_shoot" "blast_dir" x}			{delay 0.01{stuff_detonate}}		}	}	{"mine_signal"		{on "_explosion" overload			{volumes disable all}			{spawn "flashmine_jumper" "root"}			{stuff_detonate}		}	}	{"mine_strikecore"		{on "change_state"			{if mined				{tags add "mine_planted"}				{volumes "detector_ir1" enable contact}				{volumes "det_seismo" enable contact}			else tagged "manual_install" 				{tags add "mine_planted"}				{volumes "detector_ir1" enable contact}				{if tagged "armed"{volumes "det_seismo" enable contact}}			else				{tags remove "mine_planted"}				{volumes "detector_ir1" disable contact}				{volumes "det_seismo" disable contact}			}				}		{on contact overload		  {if mined			{if mine_effector "cannon" {if volume "det_seismo" {call "_explosion"}}			else mine_effector "human" {if volume "det_seismo" {call "_explosion"}}			else mine_effector "animal" {if volume "det_seismo" {call "_explosion"}}			else mine_effector "vehicle" {call "_explosion"}}		  else tagged "manual_install"		  	{if effector "cannon" {if volume "det_seismo" {call "_explosion"}}			else effector "human" {if volume "det_seismo" {call "_explosion"}}			else effector "animal" {if volume "det_seismo" {call "_explosion"}}			else effector "vehicle"	{call "_explosion"}}		  }		}	}	{"mine_antitank_tm83"		{on spawn 		  {if mined			{bone show "box"}		  else tagged "manual_install"			{bone show "box"}		  }		}		{on "_explosion" overload			{volumes "detector_ir1" disable contact}			{delay 0.1				{spawn "tm83_core" "blast_dir" x					{impulse up 0.1 forward 100 com}				}				{spawn "spbe-d_fire" "blast_dir" x}				{delay 0.01 {stuff_detonate}}			}		}		{on "change_state"			{if mined				{tags add "mine_planted"}				{bone show "box"}			else tagged "manual_install" 				{tags add "mine_planted"}				{bone show "box"}			else				{tags remove "mine_planted"}				{bone hide "box"}			}				}		{on "arm" overload			{delay 30				{tags add "armed"}				{tags add "manual_install"}				{call "change_state"}			}			}	}		{"tm83_core"		{on spawn {delay 0.3 {delete}}}	}		{"mine_antitank_pktm1r"		{on spawn 		  {if mined			{ani_wind "open" end}		  }		}		{on "change_state"			{if mined				{tags add "mine_planted"}				{volumes "det_seismo" enable contact}				{ani_wind "open" end}			else				{tags remove "mine_planted"}				{volumes "det_seismo" disable contact}			}				}		{on contact overload		  {if mined			{if mine_effector "vehicle"			  {if volume "det_seismo"				{volumes "det_seismo" disable contact}				{volumes "detector_ir1" enable contact}				{ani_play "scan"}				{ani_play "slope"}				{delay 1.5 {ani_play "slope" -1}					{volumes "det_seismo" enable contact}					{volumes "detector_ir1" disable contact}				}			  else  volume "detector_ir1"				{volumes disable contact}				{spawn "spbe-d_fire" "foresight_fx" x}			    {spawn "spbe_pktm1r" "foresight" x {impulse forward 15 up 25 com}}			    {delete}			  }			}		  }		}	}				{"blast_restrictor"		{on spawn			{delay 0.1 {volumes disable all}{stuff_detonate}}		}	}	{"dynamite"		{on "undermine"			{tags add "fuse"}			{add_view "flash_fuse_small" "fire" "fire"}			{view start "fire"}			{ani_play "fire" 0.06}			{delay 1				{explosive 1}			}			{volumes disable touch select}			{delay 8				{call "explosion"}			}		}		{on "explosion"			{if not "explosioned"				{set "explosioned" 1}	                     				{stuff_detonate}			}		}	}	{"c4_bomb"		{on spawn		   (mod not "mp"			{link_sound "C4_bomb"				"weapon/special/C4_bomb"			}		   )		}		{on "undermine"			{tags add "fuse"}			{delay 1				{explosive 1}			}			{volumes disable touch select}			{play_sound "C4_bomb"}			{delay 1.7 {play_sound "C4_bomb"}}			{delay 3.6 {play_sound "C4_bomb"}}			{delay 8				{call "explosion"}			}		}		{on "explosion"			{if not "explosioned"				{set "explosioned" 1}	                     				{stuff_detonate}			}		}	}	{"vzriv_paket"		               		                    		  		{on blast			{if min_energy 0.3				{call "explosion"}			}		}		{on "explosion"			{stuff_detonate}		}	}	{"air_bomb"		{on "explosion"			{stuff_detonate}		}		{on spawn			{ani_play "start"}			{delay 0.5				{volumes enable contact}			}		}		{on ground_hit			{if min_speed 10				{call "explosion"}			}		}		{on contact			{if min_speed 8				{call "explosion"}			}		}	}		{"air_bomb_stuff"		{on "explosion"			{stuff_detonate}		}		{on break_armor			{call "explosion"}		}		{on contact			{if min_speed 8				{call "explosion"}			}		}	}		{"artillery_explosion"		{on "explosion"			{stuff_detonate}		}		{on spawn			{delay 0				{start_sound "weapon/shot/artillery"}				{delay 2					{call "explosion"}				}			}		}	}		{"explosion"		{on "explosion"			{stuff_detonate}		}		{on spawn			{call "explosion"}		}	}	{"cw_fgassist"		{on "explosion"			{stuff_detonate}		}		{on spawn			{impulse up 1 com}			{delay 0.1 {call "explosion"}}		}		{on contact {delete}}	}		{"explosive_barrel"		{inflammation_time 3}		{on spawn			{set "explosioned" 0}		}		{on break_armor			{if name "pierce"				{if stuff_filled "fuel" 20					{call "explosion"}				else stuff "bullet"					{if stuff_filled "water" 20						{burn fx "drops_water_small_short" fake}					}				else stuff "shell"					{call "break"}				}		else name "blast"				{call "break"}			}		}		{on contact	                         	                           	                   	     	    		}		{on "burn"			{call "break"}		}		{on "break"			{if stuff_filled "fuel" 20				{call "explosion"}			else				{spawn "car_die_particle"}				{volumes disable bullet blast contact}				{hide 0.3}			}		}		{on "explosion"			{if not "explosioned"				{set "explosioned" 1}				{volumes disable bullet blast contact}				{delay 1					{volumes enable contact}				}				{blastwave c4 5 r0 4 r1 6}				{spawn "barrel_die_big"}				{spawn "fire_shellhole"}				{add_view "smoke_barrel" "smoke" "root"}				{view start "smoke"}				{impulse up 15 3 dir 3 1 cx 0 1 cy 0 1 com}				{delay 25					{view stop "smoke"}					{hide 1}				}			}		}		{on ground_hit			{if "explosioned"				{blastwave c4 2 r0 4 r1 6}				{spawn "barrel_die" offset 0 0 7}				{delete}			}		}	}		{"balloon"		{on break_armor			{if name "pierce"				{if rand 0.2					{bullet_detonate}					{if stuff_filled "fuel" 5						{call "explosion"}					}				}			}		}		{on "explosion"			{if not "explosioned"				{set "explosioned" 1}				{spawn "flamer_die"}				{spawn "fire_molot_ground"}				{blastwave c4 10 r0 1 r1 1.5}				{fire radius 6 time 2 heating 6}				{delete}			}		}	}		{"explosive_stuff"		{inflammation_time 7}		{on break_armor			{call "explosion"}		}		{on "burn"			{call "explosion"}		}		{on "explosion"			{if not "explosioned"				{set "explosioned" 1}	                     				{stuff_detonate}			}		}	}	