{selector
	{unit "human"}

	{group
	    {ignore {class proj_rus_antirad}}
		{ignore {class proj_usa_antirad}}
		{sort {class airborne}}
		{sort {class Helicopter}}
		{sort {class small_UAV}}
		{sort {class human}}
		{sort {class tank}}
		{sort {class armoredcar}}
		{sort {class car}}
		
		{sort {class cannon}}

		{sort distance}
	}
}

